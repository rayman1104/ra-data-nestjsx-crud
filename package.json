{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test:integration": "cd test/integration && npm test",
    "start:backend": "cd test/integration && npm start",
    "start:app": "cd test/app && npm run dev"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^18.6.5",
    "lerna": "^5.0.0"
  }
}
